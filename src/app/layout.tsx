export const metadata = {
  title: 'Password Reset Demo',
  description: 'Password reset functionality demo',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
